# Paths are relative to the repository root. Environment values override defaults.
PYTHON="${PYTHON:-python}"
DEVICE="${DEVICE:-cuda:0}"
SAPROT_DIR="${SAPROT_DIR:-data/weights/backbones/SaProt_650M_PDB}"
CHECKPOINT="${CHECKPOINT:-data/weights/ppi/ppi_full/weights.pt}"
REFERENCE_BANK="${REFERENCE_BANK:-data/weights/ppi/ppi_full/reference_bank.npz}"
INPUT="${INPUT:-data/examples/training/ppi_inference.json}"
OUTPUT="${OUTPUT:-data/predictions/ppi.json}"
